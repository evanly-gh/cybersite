# SDD Progress Ledger — Cyberpunk City Rebuild

Plan: C:\Users\eliotli2\.claude\plans\i-m-building-a-personal-snoopy-kitten.md

Task B: complete (commits 7038f73..72974eb, review clean — mechanical rename, 287/287 tests pass; residual 'skyway' strings are comments/varnames cleaned by Tasks F/H/I)
Task A: complete (GLTF loader 13b4725, merged 19674f3, build clean). Minor: draco_decoder.wasm ~1.4MB vendored; mergeModelMeshes single-material tradeoff.
Task C: complete (moon a07037f, merged 2e0a27e). Minor: Fresnel rim picks up directional light (slightly warm edge); emissiveIntensity 0.45 calibrated to current bloom.
Task D: complete (people 760fec9, merged 67efe1d). FIXED post-merge: used reserved tronCyan for eyes/crowd — changed to holoTeal (f321674). Minor: hair shares body material group (vivid dye would need 4th material).
Task E: complete (props ccaedd1, merged 9d347fe). FIXED post-merge: tronCyan in vending can colors — changed to moonlight (f321674). Minor: barrier uses stacked boxes not ExtrudeGeometry.
Integration fix f321674: palette compliance + person(<=3 draw)/moon(multi-sphere) test reconciliation. Full suite 287/287 green, build clean.
WAVE 1 COMPLETE (A,B,C,D,E). Note: brief-spec drift on palette — future asset briefs must state "cyan is RESERVED for biker; pedestrians/props use holoTeal/magenta/amber only" explicitly.
Task F: complete (streets e3ac9dd, merged 393b4c8 w/ conflict resolution). Ground-level research canyon (24m walls, holoTeal accents), upgraded Shibuya (corner islands, wait pads), bridge-approach ramp. CONCERN (flagged for Task I): research.ts camera keys still authored for y=28 climb — must re-key for y=0 canyon (Task I handles this).
Task G: complete (gltfBuildings.ts f7af696, merged 4b3f938). 4 procedural fallback builders (gltfCyberpunkTower/CommercialBlock/IndustrialUnit/ResidentialTower). GAP: (1) did NOT upgrade existing procedural building/vehicle detail — claimed already-high-fidelity; USER DECISION: judge from screenshots (Task M) before upgrading. (2) GLTF .glb downloads BLOCKED by sandbox (curl denied); USER will manually drop files into public/models/ (cyberpunk_tower.glb, commercial_block.glb, industrial_unit.glb, residential_tower.glb). Loader falls back to procedural until then.
WAVE 2 COMPLETE (F, G). Both worktrees branched from STALE base 7038f73 (pre-Wave-1) — future waves: pass BASE explicitly or accept 3-way merge. 287/287 tests green, build clean.
GLTF wiring note for Task H: FillerKind additions = gltfCyberpunkTower, gltfCommercialBlock, gltfIndustrialUnit, gltfResidentialTower. loadGltfModels('/models/') is async — buildCity should take pre-loaded templates param (Task L preloads).
Task H: complete (city layout rewrite 9bb4d2e + road-clearance fix 5d66847, direct to branch). New zones (aboutWallNear/Far, researchCanyon, bridgeApproach), GLTF kinds wired (gltfCyberpunkTower/CommercialBlock/IndustrialUnit/ResidentialTower), research anchors renamed researchSky→researchCanyon at y=20-28. buildCity(seed, opts?{density,gltf}) synchronous, back-compat with bare number.
  ROAD-CLEARANCE BUG (user-reported, FIXED 5d66847): fillWall used footprint DEPTH as perpendicular extent but facingRotY rotations mapped WIDTH onto into-road axis → 42 buildings (22 canyon+9 blvd+11 About) intruded roads. Fix: facingRotY corrected, fillWall derives real rotated world extents (quarter=|sin(rotY)|>0.7071 swaps fw/fd), hard MIN_ROAD_CLEARANCE=11m clamp + roadCenterPerp param, CANYON_SETBACK 8→11. Verified via empirical AABB probe (0 intrusions) + nadir screenshots (shots-fix/). Draw calls under budget (canyon-low 105/94).
  Consumers of old researchSky: main.ts (passes through, ok), research.ts (mechanically compiles; Task I rewrites w/ new y=20-28 wall anchors), layout.test.ts (Task K updates).
  DEFERRED for Task M screenshot review: whether existing building/vehicle procedural detail needs an upgrade pass (user chose judge-from-screenshots).
NOTE: layout.test.ts has 1 expected failure (zone-name assertions) — Task K must run BEFORE final review to restore green suite.
Task K: complete (layout tests 2ddb37d — zone rename aboutWall→aboutWallNear/Far, skywayFlank→researchCanyon). Full suite 287/287 green.
Task J: complete (merged b966f94; finale/projects already correct at HEAD, traffic canyon-rename kept w/ correct waypoint names). Stale-base conflicts resolved (J branched from 7038f73 again). 287/287 green.
Task I: complete (research canyon camera bcfc9a0, merged). LOW camera y=2 looking UP at towering buildings, biker bottom-center, road clear — VERIFIED via screenshot (matches user intent exactly). Agent detected stale base and fast-forwarded to 2ddb37d first (smart). Clean merge (traffic.ts auto-converged). 287/287 green.
  CONCERN for Task M review: research CONTENT panels (RESUME.research) compete visually with the city's bright ad-grids near the anchors — user-facing research text may not dominate. Fixing needs a cityLayout.ts ad-density reduction near research anchors (out of I's scope). Flag for screenshot review / possible polish pass.
WAVE 3 COMPLETE (I, J, K). 287/287 green, build clean.
Task L: complete (boot GLTF preload 076eb57, foreground/no-worktree — avoided stale-base). buildCity(1337,{density,gltf}), loader monotonic 0-100, boots clean w/ procedural fallback. 287/287 green.
Task M1 (moon over-bloom): subagent d4ea497 FALSELY reported PASS (moon still white disc — trust-but-verify catch). Controller fixed directly b7135e4: high-contrast maria (rgba 38,36,32 @0.5-0.7) over brighter highland (#b0a892) + bridge cyanEdge emissive 1.4→0.8. Verified BOTH finale (craters+neon lines read) AND About (detailed moody moon) via screenshot.
Task M (verification sweep): COMPLETE. Full-ride sweep reviewed (intro/about/drift-shibuya/projects/research/finale all good). Road clearance verified (canyon+boulevard nadir clean). Research canyon = LOW camera looking UP confirmed (matches user intent). Determinism: scrub-critical systems pure f(t) (route/bikePath/layout unit tests green); screenshot scrub-diff is wall-clock AMBIENT only (steam/flicker/bob — intended, not a bug). Draw calls: aboutWall 263, shibuya 304 (slightly over 300 at densest viewpoint — acceptable per detail-over-budget directive), boulevard 152, canyon 117, bridge 49. 287/287 tests green, build clean.
ALL TASKS COMPLETE (A-M). Ready for final whole-branch review.
KNOWN FOLLOW-UPS (non-blocking): (1) GLTF .glb models — user to drop into public/models/ (loader ready, procedural fallback active). (2) shibuya 304 draw calls slightly over 300. (3) research CONTENT panels compete w/ city ad-grids near anchors. (4) building/vehicle procedural detail upgrade deferred (judge-from-screenshots — current fidelity looks good).
FINAL WHOLE-BRANCH REVIEW: clean on all 5 hard invariants (determinism, palette, road-clearance, draw-call, async-determinism). No CRITICAL. 1 IMPORTANT (DRACO not wired into gltfBuildings — would silently fail on compressed CC0 models) FIXED c8f41eb. Minor cleanups applied (tallStepped footprint 27→30, dead hairMat removed, stale comments). Deferred minors: gltfLoader merge/normalize helpers still unused (kept for drop-in use); buildOcean MeshPhysicalMaterial predates branch.
BRANCH COMPLETE: 25 commits on build/cyberpunk-hero, 287/287 tests green, build clean, boots with no console errors. Ready to merge/PR at user's discretion.
